/**
 * @author Ikaros Kappler
 * @date 2013-12-19
 * @version 1.0.0
 **/


IKRS.PointSet = function( points ) {

    IKRS.Object.call( this );

    if( points == undefined )
	points = [];

    this.points = points;
}


IKRS.PointSet.prototype.addPoint( p ) {
    this.points.push( p );
}

IKRS.